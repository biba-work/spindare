# --- Spindare Core ---
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# --- Native Build Artifacts ---
# We ignore the build results, but keep the source code
ios/build/
android/build/
android/.gradle/
android/app/build/
.kotlin/

# --- SECRETS & KEYS (Crucial for the "Brains") ---
# This protects your API keys and signing certificates
.env
.env*.local
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem
secrets.json

# --- Native Folders ---
# If you are manually editing native code for your algorithm, 
# make sure these are NOT ignored so they sync to GitHub.
# /ios 
# /android 

# --- System & Debug ---
.DS_Store
*.orig.*
.metro-health-check*
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo
